# 第4章：控制流程

## 目录
- [4.1 什么是控制流程？](#41-什么是控制流程)
- [4.2 条件语句：if, elif, else](#42-条件语句if-elif-else)
- [4.3 循环语句：while 循环](#43-循环语句while-循环)
- [4.4 循环语句：for 循环](#44-循环语句for-循环)
- [4.5 循环控制：break, continue, pass](#45-循环控制break-continue-pass)
- [4.6 循环嵌套](#46-循环嵌套)
- [4.7 异常处理：try, except, finally](#47-异常处理try-except-finally)
- [4.8 综合示例](#48-综合示例)
- [4.9 本章练习](#49-本章练习)

---

## 4.1 什么是控制流程？

### 程序执行的三种基本结构
1. **顺序结构**：代码从上到下依次执行
2. **选择结构**：根据条件选择执行不同的代码块
3. **循环结构**：重复执行某段代码直到条件不满足

### 控制流程的重要性
控制流程让程序能够：
- 根据不同情况做出决策
- 重复执行任务而不需要重复编写代码
- 处理错误和异常情况
- 创建复杂的逻辑和算法

```python
# 顺序结构示例
print("步骤1: 准备材料")  # 第一步
print("步骤2: 开始制作")  # 第二步
print("步骤3: 完成作品")  # 第三步

# 选择结构示例（后面详细讲解）
temperature = 25
if temperature > 30:
    print("天气炎热")
else:
    print("天气适宜")

# 循环结构示例（后面详细讲解）
for i in range(3):
    print(f"这是第 {i+1} 次循环")
```

---

## 4.2 条件语句：if, elif, else

### 4.2.1 基本 if 语句
```python
# 最简单的 if 语句
age = 18

if age >= 18:
    print("你已经成年了！")
    print("可以参加成人活动。")  # 注意缩进

print("程序继续执行...")  # 不在if代码块内

# 没有花括号，靠缩进定义代码块
if age < 18:
    print("你还未成年")
print("这条语句总会执行")  # 注意：缩进很重要！
```

### 4.2.2 if-else 语句
```python
# if-else 二选一
score = 85

if score >= 60:
    print(f"成绩 {score} 分：及格")
    print("恭喜你！")
else:
    print(f"成绩 {score} 分：不及格")
    print("需要补考")

# 多行条件语句
temperature = 28
is_sunny = True

if temperature > 25 and is_sunny:
    print("天气很好，适合外出")
    print("记得涂防晒霜")
else:
    print("今天不太适合外出")
    print("建议室内活动")
```

### 4.2.3 if-elif-else 多分支
```python
# 多个条件判断
score = 78

if score >= 90:
    grade = "A"
    print("优秀！")
elif score >= 80:  # 注意：elif 是 else if 的缩写
    grade = "B"
    print("良好")
elif score >= 70:
    grade = "C"
    print("中等")
elif score >= 60:
    grade = "D"
    print("及格")
else:
    grade = "F"
    print("不及格")

print(f"你的成绩等级是: {grade}")

# 更多 elif 示例
day = 3

if day == 1:
    print("星期一")
elif day == 2:
    print("星期二")
elif day == 3:
    print("星期三")
elif day == 4:
    print("星期四")
elif day == 5:
    print("星期五")
elif day == 6:
    print("星期六")
elif day == 7:
    print("星期日")
else:
    print("无效的日期")
```

### 4.2.4 嵌套 if 语句
```python
# if 语句可以嵌套
age = 20
has_license = True
is_sober = True

if age >= 18:
    print("年龄符合要求")
    
    if has_license:
        print("有驾驶证")
        
        if is_sober:
            print("没有饮酒")
            print("可以安全驾驶")
        else:
            print("检测到饮酒，禁止驾驶")
    else:
        print("没有驾驶证，不能驾驶")
else:
    print("年龄不足，不能驾驶")

# 使用逻辑运算符简化嵌套
if age >= 18 and has_license and is_sober:
    print("所有条件满足，可以安全驾驶")
else:
    print("条件不满足，不能驾驶")
```

### 4.2.5 条件表达式（三元运算符）
```python
# 传统写法
age = 20
if age >= 18:
    status = "成年人"
else:
    status = "未成年人"

# 使用三元运算符（更简洁）
status = "成年人" if age >= 18 else "未成年人"
print(f"状态: {status}")

# 更多示例
score = 85
result = "及格" if score >= 60 else "不及格"
print(f"成绩 {score} 分：{result}")

# 嵌套的三元运算符（不建议，可读性差）
x = 10
y = 20
max_value = x if x > y else (y if y > x else "相等")
print(f"最大值: {max_value}")
```

### 4.2.6 条件判断的技巧
```python
# 1. 检查变量是否有值
name = ""
if name:  # 空字符串为 False
    print(f"你好，{name}")
else:
    print("名字不能为空")

# 检查列表是否为空
my_list = []
if my_list:
    print(f"列表有 {len(my_list)} 个元素")
else:
    print("列表为空")

# 2. 链式比较（可读性更好）
score = 85
if 80 <= score < 90:
    print("成绩在80-90之间")

# 3. 使用 in 检查成员资格
fruits = ["apple", "banana", "orange"]
fruit = "banana"
if fruit in fruits:
    print(f"{fruit} 在水果列表中")

# 4. 使用 not 取反
if fruit not in ["grape", "watermelon"]:
    print(f"{fruit} 不是葡萄或西瓜")
```

---

## 4.3 循环语句：while 循环

### 4.3.1 基本 while 循环
```python
# 最简单的 while 循环
count = 1
while count <= 5:
    print(f"这是第 {count} 次循环")
    count += 1  # 重要：更新循环条件

print("循环结束")

# 循环条件可以是任何布尔表达式
password = ""
while password != "secret123":
    password = input("请输入密码: ")
    if password != "secret123":
        print("密码错误，请重试")

print("密码正确，欢迎！")
```

### 4.3.2 无限循环与退出
```python
# 无限循环（谨慎使用！）
# while True:
#     print("无限循环...")
#     # 需要 break 语句退出

# 带退出条件的无限循环
total = 0
count = 0

print("请输入数字，输入0结束:")
while True:
    num = float(input("请输入一个数字: "))
    
    if num == 0:
        break  # 退出循环
    
    total += num
    count += 1

if count > 0:
    average = total / count
    print(f"你输入了 {count} 个数字")
    print(f"总和: {total}")
    print(f"平均值: {average:.2f}")
```

### 4.3.3 while-else 结构
```python
# while 循环可以有一个 else 分支
# 当循环正常结束（不是通过 break 退出）时执行

# 示例：查找质数
num = 7
i = 2

print(f"检查 {num} 是否是质数:")

while i < num:
    if num % i == 0:
        print(f"{num} 可以被 {i} 整除，不是质数")
        break
    i += 1
else:
    # 只有当循环正常结束（没有break）时才执行
    print(f"{num} 是质数")

# 示例：密码尝试限制
max_attempts = 3
attempts = 0
correct_password = "python123"

print("\n密码验证系统（最多尝试3次）:")

while attempts < max_attempts:
    password = input("请输入密码: ")
    attempts += 1
    
    if password == correct_password:
        print("密码正确，登录成功！")
        break
    
    print(f"密码错误，还剩 {max_attempts - attempts} 次机会")
else:
    # 如果循环结束还没有输入正确密码
    print("尝试次数过多，账户已锁定")
```

### 4.3.4 计数器模式
```python
# 模式1：递增计数器
print("递增计数器:")
i = 1
while i <= 5:
    print(f"计数: {i}")
    i += 1

# 模式2：递减计数器
print("\n递减计数器:")
i = 5
while i >= 1:
    print(f"倒计时: {i}")
    i -= 1
print("开始！")

# 模式3：步长不为1
print("\n偶数计数器:")
i = 2
while i <= 10:
    print(f"偶数: {i}")
    i += 2

# 模式4：累计求和
print("\n1到100的求和:")
total = 0
i = 1
while i <= 100:
    total += i
    i += 1
print(f"总和: {total}")
```

### 4.3.5 实际应用示例
```python
# 应用1：猜数字游戏
import random

print("猜数字游戏！")
print("=" * 30)

secret_number = random.randint(1, 100)
attempts = 0
max_attempts = 7

print("我想了一个1到100之间的数字，你有7次机会猜中它。")

while attempts < max_attempts:
    attempts += 1
    remaining = max_attempts - attempts + 1
    
    try:
        guess = int(input(f"\n第{attempts}次尝试 (还剩{remaining}次): "))
    except ValueError:
        print("请输入有效的数字！")
        continue
    
    if guess < secret_number:
        print("猜小了！")
    elif guess > secret_number:
        print("猜大了！")
    else:
        print(f"恭喜！你在第{attempts}次猜中了数字 {secret_number}！")
        break

if attempts >= max_attempts and guess != secret_number:
    print(f"\n游戏结束！你没有猜中。正确的数字是 {secret_number}。")

# 应用2：用户菜单系统
def user_menu():
    print("\n用户菜单系统")
    print("=" * 20)
    print("1. 查看个人信息")
    print("2. 修改密码")
    print("3. 查看通知")
    print("4. 退出系统")
    
    choice = input("请选择操作 (1-4): ")
    return choice

# 主程序
while True:
    choice = user_menu()
    
    if choice == "1":
        print("\n显示个人信息...")
        # 实际代码会显示用户信息
    elif choice == "2":
        print("\n修改密码功能...")
        # 实际代码会处理密码修改
    elif choice == "3":
        print("\n显示通知...")
        # 实际代码会显示通知
    elif choice == "4":
        print("感谢使用，再见！")
        break
    else:
        print("无效选择，请重试")
```

---

## 4.4 循环语句：for 循环

### 4.4.1 基本 for 循环
```python
# for 循环遍历序列（列表、字符串、元组等）

# 遍历列表
fruits = ["apple", "banana", "orange", "grape"]
print("水果列表:")
for fruit in fruits:
    print(f"  - {fruit}")

# 遍历字符串
word = "Python"
print("\n字符串字符:")
for char in word:
    print(f"  {char}")

# 遍历元组
colors = ("red", "green", "blue")
print("\n颜色元组:")
for color in colors:
    print(f"  {color}")
```

### 4.4.2 range() 函数
```python
# range() 生成数字序列

# range(stop) - 从0到stop-1
print("range(5):")
for i in range(5):
    print(f"  i = {i}")

# range(start, stop) - 从start到stop-1
print("\nrange(2, 7):")
for i in range(2, 7):
    print(f"  i = {i}")

# range(start, stop, step) - 指定步长
print("\nrange(1, 10, 2):")  # 奇数
for i in range(1, 10, 2):
    print(f"  i = {i}")

print("\nrange(10, 0, -1):")  # 递减
for i in range(10, 0, -1):
    print(f"  i = {i}")

# 实际应用：计算累加和
total = 0
for i in range(1, 101):  # 1到100
    total += i
print(f"\n1到100的和: {total}")

# 实际应用：乘法表（部分）
print("\n9的乘法表:")
for i in range(1, 10):
    print(f"  9 × {i} = {9 * i}")
```

### 4.4.3 for-else 结构
```python
# for 循环也可以有 else 分支
# 当循环正常结束（不是通过 break 退出）时执行

# 示例：查找元素
numbers = [1, 3, 5, 7, 9]
target = 6

print(f"在列表 {numbers} 中查找 {target}:")

for num in numbers:
    if num == target:
        print(f"找到了 {target}!")
        break
else:
    # 只有当循环正常结束（没有break）时才执行
    print(f"没有找到 {target}")

# 示例：检查列表是否全是偶数
def all_even(numbers):
    """检查列表中的所有数字是否都是偶数"""
    for num in numbers:
        if num % 2 != 0:
            print(f"{num} 是奇数，列表不全是偶数")
            break
    else:
        print("列表中的所有数字都是偶数")

# 测试
print("\n检查列表是否全是偶数:")
all_even([2, 4, 6, 8])  # 全是偶数
all_even([2, 4, 7, 8])  # 包含奇数
```

### 4.4.4 enumerate() 函数
```python
# enumerate() 同时获取索引和值

fruits = ["apple", "banana", "orange", "grape"]

print("普通遍历:")
for fruit in fruits:
    print(f"  水果: {fruit}")

print("\n使用 enumerate() 遍历:")
for index, fruit in enumerate(fruits):
    print(f"  索引 {index}: {fruit}")

# 指定起始索引
print("\n指定起始索引为1:")
for index, fruit in enumerate(fruits, start=1):
    print(f"  第 {index} 个水果: {fruit}")

# 实际应用：显示带编号的菜单
menu_items = ["鱼香肉丝", "宫保鸡丁", "麻婆豆腐", "回锅肉"]
print("\n今日菜单:")
for i, item in enumerate(menu_items, start=1):
    print(f"  {i}. {item}")

# 用户选择
choice = int(input("请选择菜品编号: "))
if 1 <= choice <= len(menu_items):
    print(f"你选择了: {menu_items[choice-1]}")
else:
    print("无效选择")
```

### 4.4.5 zip() 函数
```python
# zip() 同时遍历多个序列

names = ["Alice", "Bob", "Charlie"]
scores = [95, 88, 92]
grades = ["A", "B", "A"]

print("学生成绩表:")
print("姓名\t成绩\t等级")
print("-" * 20)

# 同时遍历三个列表
for name, score, grade in zip(names, scores, grades):
    print(f"{name}\t{score}\t{grade}")

# zip() 与 enumerate() 结合使用
print("\n带序号的学生成绩表:")
for i, (name, score, grade) in enumerate(zip(names, scores, grades), start=1):
    print(f"{i}. {name}: {score}分 ({grade})")

# 实际应用：计算总分和平均分
print("\n计算总分和平均分:")
total_score = 0
student_count = 0

for name, score in zip(names, scores):
    total_score += score
    student_count += 1
    print(f"{name}: {score}分")

average_score = total_score / student_count if student_count > 0 else 0
print(f"总分: {total_score}")
print(f"平均分: {average_score:.1f}")
```

### 4.4.6 字典遍历
```python
# 遍历字典的键、值、键值对

student_scores = {
    "Alice": 95,
    "Bob": 88,
    "Charlie": 92,
    "David": 78,
    "Eve": 85
}

print("学生成绩字典:")
print(student_scores)

# 遍历键
print("\n学生姓名（键）:")
for name in student_scores:
    print(f"  {name}")

# 等价写法
for name in student_scores.keys():
    print(f"  {name}")

# 遍历值
print("\n学生成绩（值）:")
for score in student_scores.values():
    print(f"  {score}分")

# 遍历键值对
print("\n学生姓名和成绩（键值对）:")
for name, score in student_scores.items():
    print(f"  {name}: {score}分")

# 实际应用：找出最高分和最低分
if student_scores:
    max_score = max(student_scores.values())
    min_score = min(student_scores.values())
    
    # 找出获得最高分的学生
    top_students = [name for name, score in student_scores.items() 
                   if score == max_score]
    
    # 找出获得最低分的学生
    bottom_students = [name for name, score in student_scores.items() 
                      if score == min_score]
    
    print(f"\n最高分: {max_score}分 - {', '.join(top_students)}")
    print(f"最低分: {min_score}分 - {', '.join(bottom_students)}")
```

---

## 4.5 循环控制：break, continue, pass

### 4.5.1 break 语句
```python
# break: 立即退出当前循环

print("break 语句示例:")
print("=" * 30)

# 示例1：找到第一个符合条件的元素就停止
numbers = [1, 3, 5, 8, 10, 12, 15]
target = 10

for num in numbers:
    if num == target:
        print(f"找到了目标数字 {target}")
        break
    print(f"检查 {num}...")
else:
    print("没有找到目标数字")

# 示例2：密码验证（最多尝试3次）
print("\n密码验证:")
correct_password = "python123"
attempts = 0

while True:
    password = input("请输入密码: ")
    attempts += 1
    
    if password == correct_password:
        print("密码正确！")
        break
    
    if attempts >= 3:
        print("尝试次数过多，退出程序")
        break
    
    print(f"密码错误，还剩 {3-attempts} 次机会")

# 示例3：多层循环中的 break（只退出当前循环）
print("\n多层循环中的 break:")
for i in range(3):
    print(f"\n外层循环 i = {i}")
    
    for j in range(5):
        if j == 3:
            print(f"  内层循环 j = {j}，退出内层循环")
            break
        print(f"  内层循环 j = {j}")
```

### 4.5.2 continue 语句
```python
# continue: 跳过本次循环的剩余代码，进入下一次循环

print("continue 语句示例:")
print("=" * 30)

# 示例1：只处理偶数
numbers = range(1, 11)

print("1-10中的偶数:")
for num in numbers:
    if num % 2 != 0:  # 如果是奇数
        continue  # 跳过后续代码
    print(f"  {num} 是偶数")

# 示例2：过滤无效数据
data = [10, 0, 25, -5, 30, "abc", 15, None, 40]

print("\n处理数据（忽略无效值）:")
total = 0
count = 0

for value in data:
    # 如果不是数字，跳过
    if not isinstance(value, (int, float)):
        print(f"  跳过非数字: {value}")
        continue
    
    # 如果是负数，跳过
    if value < 0:
        print(f"  跳过负数: {value}")
        continue
    
    # 如果是0，跳过
    if value == 0:
        print(f"  跳过0")
        continue
    
    # 处理有效数据
    total += value
    count += 1
    print(f"  处理有效数据: {value}")

if count > 0:
    average = total / count
    print(f"\n有效数据: {count} 个")
    print(f"总和: {total}")
    print(f"平均值: {average:.1f}")

# 示例3：跳过特定字符
text = "Hello, World! Python is awesome."
vowels = "aeiouAEIOU"

print("\n原文本:", text)
print("去除元音字母后:")

result = ""
for char in text:
    if char in vowels:
        continue
    result += char

print(result)
```

### 4.5.3 pass 语句
```python
# pass: 空语句，什么都不做，占位符

print("pass 语句示例:")
print("=" * 30)

# 示例1：函数或类的占位符
def calculate_something():
    # TODO: 实现这个函数
    pass  # 先占位，避免语法错误

class MyClass:
    # TODO: 添加类的方法
    pass  # 先占位

# 示例2：条件分支的占位符
age = 25

if age < 18:
    print("未成年人")
elif 18 <= age < 60:
    # 成年人处理逻辑，还没想好怎么写
    pass  # 先占位，以后补充
else:
    print("老年人")

# 示例3：循环中的占位符
for i in range(5):
    if i == 2:
        pass  # 对i==2的情况，暂时什么都不做
    else:
        print(f"i = {i}")

# 实际对比：break、continue、pass的区别
print("\nbreak、continue、pass的区别:")
for i in range(1, 6):
    if i == 3:
        print(f"  i={i}: 使用break，退出循环")
        break
    print(f"  i={i}")

print("\n---")
for i in range(1, 6):
    if i == 3:
        print(f"  i={i}: 使用continue，跳过此次循环")
        continue
    print(f"  i={i}")

print("\n---")
for i in range(1, 6):
    if i == 3:
        print(f"  i={i}: 使用pass，什么都不做，继续执行")
        pass
    print(f"  i={i}")
```

---

## 4.6 循环嵌套

### 4.6.1 基本循环嵌套
```python
# 循环可以嵌套，内层循环会在外层循环的每次迭代中完整执行

print("循环嵌套示例：乘法表")
print("=" * 30)

# 打印九九乘法表
for i in range(1, 10):  # 外层循环：被乘数
    for j in range(1, i + 1):  # 内层循环：乘数
        product = i * j
        print(f"{j}×{i}={product:2}", end="  ")  # :2 表示占2个字符宽度
    print()  # 换行

# 打印图案
print("\n打印星号三角形:")
rows = 5

for i in range(1, rows + 1):
    # 打印空格
    for j in range(rows - i):
        print(" ", end="")
    
    # 打印星号
    for k in range(2 * i - 1):
        print("*", end="")
    
    print()  # 换行
```

### 4.6.2 列表推导式中的嵌套循环
```python
# 列表推导式也支持嵌套循环

# 生成乘法表结果列表
multiplication_table = [f"{i}×{j}={i*j}" 
                       for i in range(1, 4) 
                       for j in range(1, 4)]

print("3×3乘法表结果:")
for item in multiplication_table:
    print(f"  {item}")

# 生成坐标点
coordinates = [(x, y) 
              for x in range(1, 4) 
              for y in range(1, 4)]

print("\n坐标点 (x, y):")
for coord in coordinates:
    print(f"  {coord}")

# 条件过滤
even_products = [i * j 
                for i in range(1, 4) 
                for j in range(1, 4) 
                if (i * j) % 2 == 0]

print("\n偶数乘积:")
print(even_products)
```

### 4.6.3 实际应用：查找组合
```python
# 应用：查找数组中两个数的和等于目标值
def find_two_sum(numbers, target):
    """在数组中查找两个数的和等于目标值"""
    print(f"在数组 {numbers} 中查找和为 {target} 的两个数:")
    
    for i in range(len(numbers)):
        for j in range(i + 1, len(numbers)):
            if numbers[i] + numbers[j] == target:
                print(f"  找到: {numbers[i]} + {numbers[j]} = {target}")
                return (i, j)  # 返回索引
    
    print("  没有找到符合条件的两个数")
    return None

# 测试
numbers = [2, 7, 11, 15, 3, 6]
target = 9
result = find_two_sum(numbers, target)

# 应用：矩阵操作
print("\n矩阵操作:")
matrix = [
    [1, 2, 3],
    [4, 5, 6],
    [7, 8, 9]
]

print("原始矩阵:")
for row in matrix:
    print(f"  {row}")

# 转置矩阵
print("\n转置矩阵:")
transposed = []
for i in range(len(matrix[0])):  # 列数
    new_row = []
    for j in range(len(matrix)):  # 行数
        new_row.append(matrix[j][i])
    transposed.append(new_row)

for row in transposed:
    print(f"  {row}")
```

### 4.6.4 使用 itertools 模块
```python
# 对于复杂的循环嵌套，可以使用 itertools 模块

import itertools

# 排列
letters = ['A', 'B', 'C']
print("排列 (顺序重要):")
for perm in itertools.permutations(letters, 2):
    print(f"  {perm}")

# 组合
print("\n组合 (顺序不重要):")
for comb in itertools.combinations(letters, 2):
    print(f"  {comb}")

# 笛卡尔积（相当于嵌套循环）
colors = ['红', '绿']
sizes = ['S', 'M', 'L']

print("\n笛卡尔积 (颜色 × 尺寸):")
for color, size in itertools.product(colors, sizes):
    print(f"  {color}{size}号")

# 相当于：
# for color in colors:
#     for size in sizes:
#         print(f"  {color}{size}号")
```

---

## 4.7 异常处理：try, except, finally

### 4.7.1 基本异常处理
```python
# try-except：捕获和处理异常

print("基本异常处理:")
print("=" * 30)

# 示例1：除零错误
try:
    result = 10 / 0
    print(f"结果: {result}")
except ZeroDivisionError:
    print("错误：除数不能为零！")

# 示例2：类型错误
try:
    number = int("abc")  # 无法转换为整数
    print(f"数字: {number}")
except ValueError as e:
    print(f"类型错误: {e}")

# 示例3：多个 except 块
try:
    num = int(input("请输入一个数字: "))
    result = 100 / num
    print(f"100 / {num} = {result}")
except ValueError:
    print("错误：请输入有效的数字！")
except ZeroDivisionError:
    print("错误：除数不能为零！")
except Exception as e:
    print(f"发生了未知错误: {e}")
```

### 4.7.2 else 和 finally 子句
```python
# try-except-else-finally 完整结构

print("完整的异常处理结构:")
print("=" * 30)

def safe_division(a, b):
    """安全的除法函数"""
    try:
        result = a / b
    except ZeroDivisionError:
        print("错误：除数不能为零！")
        return None
    except TypeError:
        print("错误：参数类型不正确！")
        return None
    else:
        # 只有当try块没有抛出异常时才执行
        print(f"计算成功: {a} / {b} = {result}")
        return result
    finally:
        # 无论是否发生异常都会执行
        print("除法计算完成")
        print("-" * 20)

# 测试
print("测试1: 正常计算")
safe_division(10, 2)

print("\n测试2: 除零错误")
safe_division(10, 0)

print("\n测试3: 类型错误")
safe_division(10, "2")
```

### 4.7.3 自定义异常
```python
# 创建自定义异常类

class InvalidAgeError(Exception):
    """无效年龄异常"""
    def __init__(self, age, message="年龄必须在0-150之间"):
        self.age = age
        self.message = message
        super().__init__(self.message)
    
    def __str__(self):
        return f"{self.message}: {self.age}"

class Student:
    """学生类"""
    def __init__(self, name, age):
        self.name = name
        
        # 验证年龄
        if not (0 <= age <= 150):
            raise InvalidAgeError(age)
        self.age = age
    
    def __str__(self):
        return f"{self.name} ({self.age}岁)"

# 使用自定义异常
print("自定义异常示例:")
print("=" * 30)

try:
    # 创建学生对象
    student1 = Student("Alice", 20)
    print(f"创建成功: {student1}")
    
    # 尝试创建年龄无效的学生
    student2 = Student("Bob", 200)
    print(f"创建成功: {student2}")
    
except InvalidAgeError as e:
    print(f"错误: {e}")
except Exception as e:
    print(f"未知错误: {e}")
```

### 4.7.4 异常处理的最佳实践
```python
# 异常处理的最佳实践

print("异常处理最佳实践:")
print("=" * 30)

# 实践1：具体异常优于通用异常
def read_file_bad(filename):
    """不好的写法：捕获所有异常"""
    try:
        with open(filename, 'r') as file:
            content = file.read()
        return content
    except Exception:  # 太宽泛了！
        return None

def read_file_good(filename):
    """好的写法：捕获具体异常"""
    try:
        with open(filename, 'r') as file:
            content = file.read()
        return content
    except FileNotFoundError:
        print(f"文件不存在: {filename}")
        return None
    except PermissionError:
        print(f"没有权限读取文件: {filename}")
        return None
    except UnicodeDecodeError:
        print(f"文件编码错误: {filename}")
        return None

# 实践2：不要静默忽略异常
def process_data_bad(data):
    """不好的写法：静默忽略异常"""
    try:
        result = complex_calculation(data)
        return result
    except Exception:
        pass  # 静默忽略，很难调试！

def process_data_good(data):
    """好的写法：记录或重新抛出异常"""
    try:
        result = complex_calculation(data)
        return result
    except ValueError as e:
        print(f"数据格式错误: {e}")
        # 返回默认值或重新抛出
        raise
    except Exception as e:
        print(f"未知错误: {e}")
        # 记录日志
        import logging
        logging.error(f"处理数据时出错: {e}")
        raise

# 实践3：使用上下文管理器（with语句）
print("\n使用 with 语句自动处理资源:")

# 传统写法（容易忘记关闭文件）
file = open("example.txt", "w")
try:
    file.write("Hello, World!")
finally:
    file.close()  # 必须记得关闭

# 使用 with 语句（自动关闭）
with open("example.txt", "w") as file:
    file.write("Hello, World!")
# 文件自动关闭，无需手动调用 close()
```

---

## 4.8 综合示例

### 示例1：学生成绩管理系统
```python
"""
学生成绩管理系统
"""

def grade_management_system():
    """学生成绩管理系统主函数"""
    students = {}  # 存储学生信息：{姓名: 成绩列表}
    
    print("学生成绩管理系统")
    print("=" * 40)
    
    while True:
        print("\n请选择操作:")
        print("1. 添加学生成绩")
        print("2. 查看所有学生")
        print("3. 查看学生详情")
        print("4. 删除学生")
        print("5. 成绩统计")
        print("6. 退出系统")
        
        choice = input("请输入选项 (1-6): ")
        
        if choice == "1":
            add_student(students)
        elif choice == "2":
            view_all_students(students)
        elif choice == "3":
            view_student_detail(students)
        elif choice == "4":
            delete_student(students)
        elif choice == "5":
            show_statistics(students)
        elif choice == "6":
            print("感谢使用，再见！")
            break
        else:
            print("无效选项，请重试")

def add_student(students):
    """添加学生成绩"""
    try:
        name = input("请输入学生姓名: ").strip()
        
        if not name:
            print("姓名不能为空！")
            return
        
        scores_input = input(f"请输入 {name} 的成绩（用空格分隔）: ")
        scores = [float(score) for score in scores_input.split()]
        
        if name in students:
            # 如果学生已存在，添加新成绩
            students[name].extend(scores)
            print(f"已为 {name} 添加 {len(scores)} 个新成绩")
        else:
            # 新学生
            students[name] = scores
            print(f"已添加学生 {name}，共 {len(scores)} 个成绩")
    
    except ValueError:
        print("错误：成绩必须是数字！")
    except Exception as e:
        print(f"添加学生时出错: {e}")

def view_all_students(students):
    """查看所有学生"""
    if not students:
        print("还没有学生信息")
        return
    
    print(f"\n共有 {len(students)} 名学生:")
    print("-" * 40)
    
    for i, (name, scores) in enumerate(students.items(), start=1):
        avg_score = sum(scores) / len(scores) if scores else 0
        print(f"{i}. {name}: {len(scores)} 个成绩，平均分 {avg_score:.1f}")

def view_student_detail(students):
    """查看学生详情"""
    name = input("请输入要查看的学生姓名: ").strip()
    
    if name not in students:
        print(f"找不到学生: {name}")
        return
    
    scores = students[name]
    print(f"\n学生: {name}")
    print(f"成绩数量: {len(scores)}")
    
    if scores:
        print("所有成绩:", " ".join(str(s) for s in scores))
        print(f"最高分: {max(scores)}")
        print(f"最低分: {min(scores)}")
        print(f"平均分: {sum(scores) / len(scores):.1f}")
        
        # 成绩分布
        excellent = len([s for s in scores if s >= 90])
        good = len([s for s in scores if 80 <= s < 90])
        medium = len([s for s in scores if 70 <= s < 80])
        pass_grade = len([s for s in scores if 60 <= s < 70])
        fail = len([s for s in scores if s < 60])
        
        print(f"\n成绩分布:")
        print(f"  优秀 (≥90): {excellent} 个")
        print(f"  良好 (80-89): {good} 个")
        print(f"  中等 (70-79): {medium} 个")
        print(f"  及格 (60-69): {pass_grade} 个")
        print(f"  不及格 (<60): {fail} 个")
    else:
        print("该学生暂无成绩")

def delete_student(students):
    """删除学生"""
    name = input("请输入要删除的学生姓名: ").strip()
    
    if name not in students:
        print(f"找不到学生: {name}")
        return
    
    confirm = input(f"确定要删除 {name} 吗？(y/n): ").lower()
    if confirm == 'y':
        del students[name]
        print(f"已删除学生: {name}")
    else:
        print("取消删除")

def show_statistics(students):
    """显示统计信息"""
    if not students:
        print("还没有学生信息")
        return
    
    all_scores = []
    for scores in students.values():
        all_scores.extend(scores)
    
    if not all_scores:
        print("暂无成绩数据")
        return
    
    print("\n系统统计信息:")
    print("=" * 40)
    print(f"学生总数: {len(students)}")
    print(f"成绩总数: {len(all_scores)}")
    print(f"平均分（所有学生）: {sum(all_scores) / len(all_scores):.1f}")
    print(f"最高分: {max(all_scores)}")
    print(f"最低分: {min(all_scores)}")
    
    # 各分数段统计
    grade_ranges = [(90, 100), (80, 89), (70, 79), (60, 69), (0, 59)]
    grade_names = ["优秀", "良好", "中等", "及格", "不及格"]
    
    print("\n分数段分布:")
    for (low, high), name in zip(grade_ranges, grade_names):
        if high == 100:
            count = len([s for s in all_scores if s >= low])
        else:
            count = len([s for s in all_scores if low <= s <= high])
        
        percentage = count / len(all_scores) * 100 if all_scores else 0
        print(f"  {name} ({low}-{high}分): {count} 个 ({percentage:.1f}%)")

# 运行系统
if __name__ == "__main__":
    grade_management_system()
```

### 示例2：猜数字游戏（增强版）
```python
"""
增强版猜数字游戏
"""

import random
import time

class GuessNumberGame:
    """猜数字游戏类"""
    
    def __init__(self, min_num=1, max_num=100, max_attempts=10):
        self.min_num = min_num
        self.max_num = max_num
        self.max_attempts = max_attempts
        self.secret_number = 0
        self.attempts = 0
        self.start_time = 0
        self.guess_history = []
    
    def generate_secret_number(self):
        """生成秘密数字"""
        self.secret_number = random.randint(self.min_num, self.max_num)
    
    def get_hint(self, guess):
        """根据猜测提供提示"""
        diff = abs(guess - self.secret_number)
        
        if diff == 0:
            return "恭喜！猜中了！"
        elif diff <= 5:
            return "非常接近！"
        elif diff <= 15:
            return "接近了"
        elif diff <= 30:
            return "有点远"
        else:
            return "差很远"
    
    def play_round(self):
        """进行一轮游戏"""
        self.generate_secret_number()
        self.attempts = 0
        self.start_time = time.time()
        self.guess_history = []
        
        print(f"\n🎮 猜数字游戏开始！")
        print(f"📊 范围: {self.min_num} 到 {self.max_num}")
        print(f"🎯 目标: 在 {self.max_attempts} 次内猜中数字")
        print("-" * 40)
        
        while self.attempts < self.max_attempts:
            self.attempts += 1
            remaining = self.max_attempts - self.attempts + 1
            
            try:
                guess = int(input(f"📝 第{self.attempts}次尝试 (还剩{remaining}次): "))
                
                # 验证输入范围
                if guess < self.min_num or guess > self.max_num:
                    print(f"❌ 请输入 {self.min_num} 到 {self.max_num} 之间的数字")
                    self.attempts -= 1  # 不计入尝试次数
                    continue
                
                # 记录猜测历史
                self.guess_history.append(guess)
                
                # 检查猜测
                if guess < self.secret_number:
                    print(f"🔺 猜小了！ ({self.get_hint(guess)})")
                elif guess > self.secret_number:
                    print(f"🔻 猜大了！ ({self.get_hint(guess)})")
                else:
                    elapsed_time = time.time() - self.start_time
                    self.show_victory_message(elapsed_time)
                    return True
                
                # 显示猜测历史
                if len(self.guess_history) > 1:
                    print(f"📈 猜测历史: {self.guess_history}")
                
            except ValueError:
                print("❌ 请输入有效的数字！")
                self.attempts -= 1  # 不计入尝试次数
        
        # 如果没有猜中
        self.show_defeat_message()
        return False
    
    def show_victory_message(self, elapsed_time):
        """显示胜利信息"""
        print("\n" + "🎉" * 20)
        print(f"🎉 恭喜！你在第{self.attempts}次猜中了数字 {self.secret_number}！")
        print(f"⏱️  用时: {elapsed_time:.1f} 秒")
        print(f"📊 猜测历史: {self.guess_history}")
        
        # 评级
        if self.attempts <= 3:
            rating = "天才"
        elif self.attempts <= 5:
            rating = "优秀"
        elif self.attempts <= 7:
            rating = "良好"
        else:
            rating = "合格"
        
        print(f"🏆 评级: {rating}")
        print("🎉" * 20)
    
    def show_defeat_message(self):
        """显示失败信息"""
        print("\n" + "😢" * 20)
        print(f"😢 游戏结束！你没有在 {self.max_attempts} 次内猜中。")
        print(f"🎯 正确的数字是: {self.secret_number}")
        print(f"📊 你的猜测: {self.guess_history}")
        
        # 分析猜测
        if self.guess_history:
            avg_guess = sum(self.guess_history) / len(self.guess_history)
            print(f"📈 平均猜测值: {avg_guess:.1f}")
        
        print("😢" * 20)
    
    def show_statistics(self, games_played, games_won):
        """显示游戏统计"""
        if games_played > 0:
            win_rate = games_won / games_played * 100
            print(f"\n📊 游戏统计:")
            print(f"   总游戏数: {games_played}")
            print(f"   获胜数: {games_won}")
            print(f"   胜率: {win_rate:.1f}%")
    
    def run(self):
        """运行游戏"""
        print("=" * 50)
        print("          🎮 增强版猜数字游戏 🎮")
        print("=" * 50)
        
        games_played = 0
        games_won = 0
        
        while True:
            games_played += 1
            won = self.play_round()
            
            if won:
                games_won += 1
            
            self.show_statistics(games_played, games_won)
            
            # 询问是否继续
            play_again = input("\n🔄 是否再玩一局？(y/n): ").lower()
            if play_again != 'y':
                print("\n感谢游玩，再见！👋")
                break

# 运行游戏
if __name__ == "__main__":
    game = GuessNumberGame(min_num=1, max_num=100, max_attempts=10)
    game.run()
```

---

## 4.9 本章练习

### 练习1：条件判断练习
编写程序实现以下功能：
1. 输入一个年份，判断是否是闰年
   - 闰年规则：能被4整除但不能被100整除，或者能被400整除
2. 输入三个数字，找出最大值和最小值
3. 输入三角形的三边长度，判断是否能构成三角形，以及是什么三角形（等边、等腰、直角、一般）

### 练习2：循环练习
1. **while循环**：编写程序，让用户输入数字，直到输入0为止，然后计算所有输入数字的平均值
2. **for循环**：打印以下图案
   ```
       *
      ***
     *****
    *******
   *********
   ```
3. **嵌套循环**：打印九九乘法表，但是只打印下三角部分（即只打印 i>=j 的部分）

### 练习3：break/continue练习
编写程序实现：
1. 输入一个整数n，找出1到n之间所有能被3或5整除但不能同时被3和5整除的数字
2. 输入一个字符串，统计其中元音字母（a, e, i, o, u）的个数，遇到'q'则停止统计
3. 模拟登录系统，用户有3次尝试机会，如果输入正确密码则显示欢迎信息并退出，否则提示剩余次数

### 练习4：异常处理练习
1. 编写一个安全的除法计算器，处理以下异常：
   - 除零错误
   - 非数字输入
   - 输入为空
2. 编写一个文件读取程序，要求：
   - 如果文件不存在，提示用户并询问是否创建新文件
   - 如果文件存在但无法读取（权限问题），给出提示
   - 使用try-except-finally确保文件正确关闭

### 练习5：综合项目
实现一个**简易银行系统**，要求：
1. 用户可以通过菜单选择：存款、取款、查询余额、查看交易记录、退出
2. 初始余额为1000元
3. 存款和取款需要输入金额，并进行有效性检查（不能为负数，取款不能超过余额）
4. 记录每笔交易（类型、金额、时间、余额变化）
5. 使用循环让用户可以多次操作，直到选择退出
6. 使用异常处理确保程序的健壮性

### 练习6：算法挑战
1. **寻找质数**：找出1-100之间所有的质数
2. **斐波那契数列**：生成斐波那契数列的前20个数字
3. **数字反转**：输入一个整数，将其数字反转（如：12345 → 54321）
4. **最大公约数**：使用欧几里得算法计算两个数的最大公约数

### 练习7：代码调试
找出以下代码中的错误并修正：
```python
# 代码1：猜数字游戏（有错误）
import random

number = random.randint(1, 10)
attempts = 0

print("猜数字游戏！数字在1-10之间")

while attempts < 3:
    guess = input("你的猜测: ")
    
    if guess = number:
        print("恭喜！猜对了！")
        break
    elif guess < number:
        print("猜小了")
    else:
        print("猜大了")
    
    attempts = attempts + 1

print(f"游戏结束！正确答案是 {number}")

# 代码2：成绩计算器（有错误）
scores = []
print("请输入成绩，输入-1结束")

while True:
    score = input("成绩: ")
    
    if score == -1:
        break
    
    scores.append(score)

if scores:
    average = sum(scores) / scores.length()
    print(f"平均分: {average}")
else:
    print("没有输入成绩")
```

---

## 总结

本章中，你学习了：
- ✅ **条件语句**：`if`, `elif`, `else`，包括三元运算符
- ✅ **循环语句**：`while` 循环和 `for` 循环，包括 `for-else` 结构
- ✅ **循环控制**：`break`, `continue`, `pass` 的用法和区别
- ✅ **循环嵌套**：多层循环和实际应用
- ✅ **异常处理**：`try`, `except`, `else`, `finally`，以及自定义异常

**关键要点**：
1. 缩进在Python中非常重要，它定义了代码块
2. `for` 循环适合遍历已知序列，`while` 循环适合条件不确定的情况
3. 使用 `break` 和 `continue` 可以更精确地控制循环流程
4. 异常处理是编写健壮程序的关键
5. 合理使用循环嵌套可以解决复杂问题，但要注意性能

**编程建议**：
- 避免无限循环，确保循环有退出条件
- 使用 `enumerate()` 和 `zip()` 简化循环代码
- 具体异常优于通用异常，不要静默忽略异常
- 复杂逻辑考虑使用函数分解

---

## 扩展知识
- [Python 控制流程官方文档](https://docs.python.org/zh-cn/3/tutorial/controlflow.html)
- [Python 异常处理官方文档](https://docs.python.org/zh-cn/3/tutorial/errors.html)
- [itertools 模块](https://docs.python.org/zh-cn/3/library/itertools.html)

---
**下一章**：[第5章：函数]

> 如果有任何问题，欢迎在 [Issues](https://github.com/SuperF2023/python-basics-tutorial/issues) 中提问！