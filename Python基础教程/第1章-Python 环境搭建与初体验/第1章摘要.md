# 第1章：Python 环境搭建与初体验

## 目录
- [1.1 Python 简介与版本选择](#11-python-简介与版本选择)
- [1.2 安装 Python](#12-安装-python)
  - [Windows 系统](#windows-系统)
  - [macOS 系统](#macos-系统)
  - [Linux 系统](#linux-系统)
- [1.3 选择你的开发工具](#13-选择你的开发工具)
  - [1.3.1 Visual Studio Code (推荐)](#131-visual-studio-code-推荐)
  - [1.3.2 PyCharm](#132-pycharm)
  - [1.3.3 Jupyter Notebook](#133-jupyter-notebook)
- [1.4 编写第一个 Python 程序](#14-编写第一个-python-程序)
  - [1.4.1 方法一：交互式模式](#141-方法一交互式模式)
  - [1.4.2 方法二：脚本文件](#142-方法二脚本文件)
  - [1.4.3 方法三：使用 IDE](#143-方法三使用-ide)
- [1.5 理解 Python 的执行过程](#15-理解-python-的执行过程)
- [1.6 常见问题与解决方法](#16-常见问题与解决方法)
- [1.7 本章练习](#17-本章练习)

---

## 1.1 Python 简介与版本选择

Python 是一种**高级、解释型**的编程语言，由吉多·范罗苏姆（Guido van Rossum）于1991年创建。

### Python 的特点：
- **简单易学**：语法接近英语，代码可读性高
- **免费开源**：任何人都可以免费使用和分发
- **跨平台**：可在 Windows、macOS、Linux 等系统上运行
- **丰富的库**：拥有庞大的标准库和第三方库
- **社区活跃**：全球有数百万开发者使用

### 版本选择建议：
目前有两个主要版本：
- **Python 2.x**：已于2020年停止维护，**不推荐学习**
- **Python 3.x**：当前主流版本，**推荐安装 Python 3.8 或更高版本**

> 💡 **提示**：本教程所有示例均基于 Python 3.x。

---

## 1.2 安装 Python

### Windows 系统

#### 方法一：官网安装（推荐）
1. 访问 [Python 官方网站](https://www.python.org/downloads/)
2. 点击黄色的 "Download Python" 按钮（会自动推荐适合你系统的版本）
3. 运行下载的安装程序
4. **重要步骤**：
   - 勾选 "Add Python to PATH"（将Python添加到系统路径）
   - 点击 "Customize installation" 可以自定义安装路径
5. 点击 "Install Now" 开始安装

#### 方法二：Microsoft Store 安装
1. 打开 Microsoft Store
2. 搜索 "Python"
3. 选择 "Python 3.x" 并点击安装

### macOS 系统

#### 方法一：官网安装
1. 访问 [Python 官方网站](https://www.python.org/downloads/)
2. 下载 macOS 版本的安装包（.pkg 文件）
3. 双击运行安装包，按照向导完成安装

#### 方法二：使用 Homebrew（推荐给开发者）
1. 打开终端（Terminal）
2. 安装 Homebrew（如果尚未安装）：
   ```bash
   /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
   ```
3. 安装 Python：
   ```bash
   brew install python
   ```

### Linux 系统

大多数 Linux 发行版已经预装了 Python，但可能是旧版本。以下是安装/升级方法：

#### Ubuntu/Debian：
```bash
sudo apt update
sudo apt install python3 python3-pip
```

#### CentOS/RHEL/Fedora：
```bash
sudo yum install python3  # CentOS/RHEL
# 或
sudo dnf install python3  # Fedora
```

### 验证安装
安装完成后，打开命令行工具（Windows: CMD/PowerShell, macOS/Linux: Terminal），输入：

```bash
python --version
# 或
python3 --version
```

如果看到类似下面的输出，说明安装成功：
```
Python 3.x
```

---

## 1.3 选择你的开发工具

### 1.3.1 Visual Studio Code (推荐)
**VS Code** 是一个轻量级但功能强大的代码编辑器，非常适合初学者。

#### 安装步骤：
1. 下载并安装 [Visual Studio Code](https://code.visualstudio.com/)
2. 安装 Python 扩展：
   - 打开 VS Code
   - 点击左侧的扩展图标（或按 `Ctrl+Shift+X`）
   - 搜索 "Python"（由 Microsoft 发布）
   - 点击安装

#### 推荐配置：
```json
// 在 settings.json 中添加
{
    "python.pythonPath": "python3",
    "editor.formatOnSave": true,
    "python.formatting.provider": "autopep8"
}
```

### 1.3.2 PyCharm
**PyCharm** 是专业的 Python IDE，有社区版（免费）和专业版。

**适合人群**：需要更强大功能的进阶学习者或专业开发者。

### 1.3.3 Jupyter Notebook
**Jupyter** 是基于浏览器的交互式笔记本，特别适合数据分析和教学。

安装方法：
```bash
pip install notebook
# 启动
jupyter notebook
```

---

## 1.4 编写第一个 Python 程序

### 1.4.1 方法一：交互式模式
打开命令行，输入 `python` 或 `python3` 进入交互模式：

```python
>>> print("Hello, World!")
Hello, World!
>>> name = input("What's your name? ")
What's your name? Alice
>>> print(f"Welcome, {name}!")
Welcome, Alice!
```

**特点**：输入一行，立即执行一行，适合快速测试。

退出交互模式：按 `Ctrl+Z`（Windows）或 `Ctrl+D`（macOS/Linux），或输入 `exit()`。

### 1.4.2 方法二：脚本文件
创建一个文本文件，保存为 `hello.py`：

```python
# hello.py
print("=" * 30)
print("我的第一个Python程序")
print("=" * 30)

# 获取用户输入
name = input("请输入你的名字: ")

# 输出欢迎信息
print(f"你好，{name}！欢迎来到Python世界！")

# 计算并输出
age = input("请输入你的年龄: ")
age = int(age)  # 将字符串转换为整数
next_year = age + 1
print(f"明年你就{next_year}岁了！")
```

**运行脚本**：
```bash
python hello.py
# 或
python3 hello.py
```

### 1.4.3 方法三：使用 IDE
以 VS Code 为例：
1. 新建文件 `hello.py`
2. 输入代码
3. 点击右上角的运行按钮 ▶️
4. 或在终端中运行：`python hello.py`

---

## 1.5 理解 Python 的执行过程

Python 是**解释型语言**，执行过程如下：

1. **编写源代码** → 保存为 `.py` 文件
2. **解释器读取** → Python 解释器逐行读取源代码
3. **编译为字节码** → 转换为中间代码（`.pyc` 文件，自动生成）
4. **执行字节码** → Python 虚拟机执行字节码
5. **输出结果**

这个过程与 C/C++ 等**编译型语言**不同，后者需要先将源代码编译成机器码。

**优点**：
- 跨平台：同一份代码可在不同系统运行
- 开发快速：无需编译，直接运行

**缺点**：
- 运行速度相对较慢
- 需要目标系统安装 Python 解释器

---

## 1.6 常见问题与解决方法

### Q1: 运行 `python` 命令显示 "不是内部或外部命令"
**原因**：Python 未添加到系统路径。
**解决**：
1. 重新安装 Python，确保勾选 "Add Python to PATH"
2. 或手动添加环境变量：
   - Windows：系统属性 → 高级 → 环境变量 → Path → 添加 Python 安装路径

### Q2: Windows 上应该用 `python` 还是 `python3`？
Windows 安装后通常使用 `python` 命令。
macOS/Linux 通常使用 `python3` 区分系统自带的 Python 2。

### Q3: 如何安装第三方库？
使用 `pip` 工具（Python 包管理器）：
```bash
pip install 库名
# 或
pip3 install 库名
```

**示例**：
```bash
pip install requests  # 安装网络请求库
```

### Q4: 多个 Python 版本如何管理？
推荐使用虚拟环境：
```bash
# 创建虚拟环境
python -m venv myenv

# 激活（Windows）
myenv\Scripts\activate

# 激活（macOS/Linux）
source myenv/bin/activate

# 退出虚拟环境
deactivate
```

---

## 1.7 本章练习

### 练习 1：基础输出
创建一个程序，输出以下内容：
```
***************
Python 学习之路
***************
我的第一个程序 [你的名字] 编写
```

### 练习 2：交互式计算器
编写一个程序，要求用户输入两个数字，然后输出：
1. 这两个数字的和
2. 这两个数字的差
3. 这两个数字的乘积
4. 这两个数字的商（保留两位小数）

**示例输出**：
```
请输入第一个数字: 10
请输入第二个数字: 3

计算结果:
10 + 3 = 13
10 - 3 = 7
10 × 3 = 30
10 ÷ 3 = 3.33
```

### 练习 3：个人信息收集
创建一个个人信息收集程序，要求输入：
1. 姓名
2. 年龄
3. 所在城市
4. 兴趣爱好（至少3个）

然后以整齐的格式输出这些信息。

### 练习 4：图案打印
使用 `print()` 函数和字符串乘法，打印以下图案：
```
*
**
***
****
*****
```

### 练习 5：错误调试
下面的代码有3处错误，请找出并修正：
```python
# buggy_code.py
print("欢迎来到Python世界)
name = input(请输入你的名字)
print("你好" + name "!")
```

---

## 总结

本章中，你完成了：
- ✅ 了解了 Python 的基本特点
- ✅ 在电脑上安装了 Python
- ✅ 选择了合适的开发工具
- ✅ 编写并运行了第一个 Python 程序
- ✅ 理解了 Python 的执行原理

**重要收获**：
- Python 安装的关键是**添加到系统路径**
- 可以从**交互模式**、**脚本文件**、**IDE**三种方式运行 Python
- `print()` 用于输出，`input()` 用于输入
- 使用 `f"{变量}"` 可以方便地格式化字符串

恭喜！你已经迈出了 Python 学习的第一步。在下一章，我们将深入学习变量和数据类型。

---

## 扩展阅读
- [Python 官方文档](https://docs.python.org/zh-cn/3/)
- [VS Code Python 教程](https://code.visualstudio.com/docs/python/python-tutorial)
- [Python 社区](https://www.python.org/community/)

---
**下一章**：[第2章：变量与数据类型](./02-variables-and-data-types.md)

> 如果有任何问题，欢迎在 [Issues](https://github.com/SuperF2023/python-basics-tutorial/issues) 中提问！