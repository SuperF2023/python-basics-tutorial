# 第2章：变量与数据类型

## 目录
- [2.1 什么是变量？](#21-什么是变量)
- [2.2 变量的命名规则](#22-变量的命名规则)
- [2.3 Python 的基本数据类型](#23-python-的基本数据类型)
  - [2.3.1 数字类型](#231-数字类型)
  - [2.3.2 字符串](#232-字符串)
  - [2.3.3 布尔类型](#233-布尔类型)
  - [2.3.4 空值类型](#234-空值类型)
- [2.4 类型查看与转换](#24-类型查看与转换)
- [2.5 动态类型与类型注解](#25-动态类型与类型注解)
- [2.6 常量的概念](#26-常量的概念)
- [2.7 综合示例](#27-综合示例)
- [2.8 本章练习](#28-本章练习)

---

## 2.1 什么是变量？

### 变量的概念
**变量**是程序中存储数据的容器。你可以把它想象成一个贴有标签的盒子：
- **盒子（变量）**：用来存放东西
- **标签（变量名）**：用来区分不同的盒子
- **里面的东西（值）**：实际存储的数据

### 变量的创建
在 Python 中，创建变量（赋值）非常简单：

```python
# 创建一个变量并赋值
message = "Hello, Python!"
score = 95
is_passed = True

# 使用变量
print(message)      # 输出: Hello, Python!
print(score)        # 输出: 95
print(is_passed)    # 输出: True
```

### 变量的特点
1. **无需声明类型**：Python 会自动推断变量类型
2. **可以重新赋值**：同一个变量可以存储不同类型的数据
3. **引用传递**：变量存储的是值的引用（地址），而不是值本身

```python
# 同一个变量可以存储不同类型的数据
x = 10          # x 是整数
print(x, type(x))  # 输出: 10 <class 'int'>

x = "现在我是字符串"  # 重新赋值为字符串
print(x, type(x))  # 输出: 现在我是字符串 <class 'str'>

x = 3.14        # 重新赋值为浮点数
print(x, type(x))  # 输出: 3.14 <class 'float'>
```

---

## 2.2 变量的命名规则

### 基本规则
1. **只能包含**：字母（a-z, A-Z）、数字（0-9）、下划线（_）
2. **不能以数字开头**
3. **区分大小写**：`age` 和 `Age` 是不同的变量
4. **不能使用关键字**（保留字）

### Python 关键字
```python
# 以下是 Python 的关键字，不能用作变量名
import keyword
print(keyword.kwlist)
```

常见的关键字有：`and`, `as`, `assert`, `break`, `class`, `continue`, `def`, `del`, `elif`, `else`, `except`, `False`, `finally`, `for`, `from`, `global`, `if`, `import`, `in`, `is`, `lambda`, `None`, `nonlocal`, `not`, `or`, `pass`, `raise`, `return`, `True`, `try`, `while`, `with`, `yield`

### 命名规范（PEP 8）
1. **小写字母 + 下划线**（snake_case，推荐）：
   ```python
   student_name = "张三"
   total_score = 100
   is_valid_input = True
   ```

2. **驼峰命名法**（CamelCase，常用于类名）：
   ```python
   className = "Python基础班"  # 小驼峰
   ClassName = "Python基础班"  # 大驼峰（PascalCase）
   ```

3. **常量**：全大写 + 下划线
   ```python
   MAX_SIZE = 100
   PI = 3.14159
   DEFAULT_TIMEOUT = 30
   ```

### 好的 vs 差的变量名
| 差的变量名 | 好的变量名 | 原因 |
|-----------|-----------|------|
| `a` | `age` | 有具体含义 |
| `n` | `student_count` | 清晰明确 |
| `flag` | `is_valid` | 布尔值用 is_ 前缀 |
| `tmp` | `temp_value` | 临时变量也要有意义 |
| `data1` | `user_data` | 避免数字后缀 |

---

## 2.3 Python 的基本数据类型

Python 有几种内置的基本数据类型，了解它们是编程的基础。

### 2.3.1 数字类型

#### 整数（int）
```python
# 整数 - 没有小数部分的数字
age = 25
students_count = 42
temperature = -5

print(type(age))  # <class 'int'>

# 不同进制的整数
binary_num = 0b1010    # 二进制：10
octal_num = 0o12       # 八进制：10
hex_num = 0xA          # 十六进制：10

print(binary_num, octal_num, hex_num)  # 10 10 10
```

#### 浮点数（float）
```python
# 浮点数 - 有小数部分的数字
price = 19.99
pi = 3.1415926535
temperature = 36.6

print(type(price))  # <class 'float'>

# 科学计数法
large_num = 1.23e5   # 1.23 × 10⁵ = 123000
small_num = 5.6e-3   # 5.6 × 10⁻³ = 0.0056

print(large_num, small_num)  # 123000.0 0.0056
```

#### 复数（complex）
```python
# 复数 - 包含实部和虚部
z = 3 + 4j
print(z.real)  # 实部: 3.0
print(z.imag)  # 虚部: 4.0
print(type(z)) # <class 'complex'>
```

### 2.3.2 字符串（str）

字符串是用引号括起来的文本。

```python
# 三种引号定义字符串
name1 = 'Alice'        # 单引号
name2 = "Bob"          # 双引号
multiline = """这是
多行
字符串"""              # 三引号（可多行）

print(name1, name2)
print(multiline)

# 字符串操作
greeting = "Hello"
name = "World"

# 1. 拼接
full_greeting = greeting + " " + name  # "Hello World"
print(full_greeting)

# 2. 重复
line = "-" * 30  # "------------------------------"
print(line)

# 3. 长度
text = "Python"
length = len(text)  # 6
print(f"'{text}' 的长度是 {length}")

# 4. 索引和切片（重要！）
word = "Python"
print(word[0])    # P - 第一个字符
print(word[-1])   # n - 最后一个字符
print(word[0:3])  # Pyt - 切片：索引0到2
print(word[2:])   # thon - 从索引2到结尾
print(word[:4])   # Pyth - 从开头到索引3
print(word[::2])  # Pto - 步长为2

# 5. 字符串方法
text = "  Hello, Python!  "
print(text.lower())        # "  hello, python!  "
print(text.upper())        # "  HELLO, PYTHON!  "
print(text.strip())        # "Hello, Python!" - 去除首尾空格
print(text.replace("Python", "World"))  # "  Hello, World!  "
print(text.find("Python")) # 9 - 找到的位置索引
```

### 2.3.3 布尔类型（bool）

布尔值只有两个：`True` 和 `False`（注意首字母大写）。

```python
# 布尔值
is_raining = True
has_permission = False

print(type(is_raining))  # <class 'bool'>

# 布尔运算
x = 10
y = 20

print(x < y)    # True
print(x == y)   # False
print(x != y)   # True
print(x >= 10)  # True

# 逻辑运算
a = True
b = False

print(a and b)  # False - 与运算（两个都为True才是True）
print(a or b)   # True  - 或运算（有一个为True就是True）
print(not a)    # False - 非运算

# 实际应用
age = 18
has_id = True

can_enter = age >= 18 and has_id
print(f"可以进入吗？{can_enter}")  # True
```

### 2.3.4 空值类型（NoneType）

`None` 表示"没有值"或"空值"，相当于其他语言中的 `null`。

```python
# None 表示空值
result = None
uninitialized = None

print(type(result))  # <class 'NoneType'>

# 判断是否为 None
if result is None:
    print("结果为空")
    
if uninitialized is not None:
    print("已初始化")
else:
    print("未初始化")  # 会执行这个
```

---

## 2.4 类型查看与转换

### 查看类型：type() 函数
```python
# 查看变量的类型
num = 42
text = "Hello"
flag = True
empty = None

print(type(num))    # <class 'int'>
print(type(text))   # <class 'str'>
print(type(flag))   # <class 'bool'>
print(type(empty))  # <class 'NoneType'>

# 使用 isinstance() 检查类型
print(isinstance(num, int))     # True
print(isinstance(text, str))    # True
print(isinstance(42.0, float))  # True
```

### 类型转换
有时我们需要在不同类型之间转换：

```python
# 1. 转换为整数 int()
price = "19.99"
num_str = "42"
pi_str = "3.14"

# 字符串转整数（字符串必须是整数格式）
number = int(num_str)  # 42
print(number, type(number))

# 浮点数转整数（会丢失小数部分）
float_num = 3.99
int_num = int(float_num)  # 3（不是四舍五入，是截断）
print(int_num)

# 2. 转换为浮点数 float()
price_float = float(price)    # 19.99
int_to_float = float(42)      # 42.0
print(price_float, int_to_float)

# 3. 转换为字符串 str()
age = 25
score = 95.5
is_pass = True

age_str = str(age)        # "25"
score_str = str(score)    # "95.5"
bool_str = str(is_pass)   # "True"
print(age_str, score_str, bool_str)

# 4. 转换为布尔值 bool()
# 以下情况会转换为 False（空值）：
# 0, 0.0, "", [], (), {}, None
# 其他情况都是 True

print(bool(0))        # False
print(bool(1))        # True
print(bool(""))       # False
print(bool("Hello"))  # True
print(bool([]))       # False
print(bool([1, 2]))   # True
print(bool(None))     # False

# 实际应用：处理用户输入
user_input = input("请输入一个数字: ")
if user_input:  # 判断输入是否为空
    number = float(user_input)
    print(f"你输入的数字是: {number}")
else:
    print("你没有输入任何内容")
```

---

## 2.5 动态类型与类型注解

### Python 的动态类型特性
Python 是动态类型语言，变量的类型可以在运行时改变：

```python
# 动态类型示例
value = 10        # 现在是整数
print(value, type(value))  # 10 <class 'int'>

value = "Hello"   # 现在是字符串
print(value, type(value))  # Hello <class 'str'>

value = [1, 2, 3] # 现在是列表
print(value, type(value))  # [1, 2, 3] <class 'list'>
```

### 类型注解（Python 3.5+）
虽然 Python 是动态类型，但我们可以使用类型注解来提高代码可读性：

```python
# 类型注解示例
from typing import List, Dict, Optional

# 变量注解
name: str = "张三"
age: int = 25
scores: List[float] = [95.5, 88.0, 92.5]
student_info: Dict[str, any] = {"name": "张三", "age": 25}

# 函数参数和返回值的注解
def calculate_average(scores: List[float]) -> float:
    """计算平均分"""
    return sum(scores) / len(scores) if scores else 0.0

# 使用 Optional 表示可能为 None
def find_student(name: str) -> Optional[Dict]:
    """查找学生信息，可能返回 None"""
    students = {"张三": {"age": 25, "score": 95}}
    return students.get(name)

# 实际调用
avg_score = calculate_average([95.5, 88.0, 92.5])
print(f"平均分: {avg_score}")

student = find_student("张三")
if student:
    print(f"找到学生: {student}")
```

> 💡 **注意**：类型注解只是提示，不会强制类型检查。可以使用 `mypy` 工具进行静态类型检查。

---

## 2.6 常量的概念

Python 没有真正的常量，但约定使用全大写变量名表示常量：

```python
# 常量约定（全大写）
PI = 3.141592653589793
MAX_USERS = 1000
DEFAULT_TIMEOUT = 30
API_KEY = "your-api-key-here"  # 实际开发中不要硬编码密钥！

# 使用常量
def calculate_circle_area(radius: float) -> float:
    """计算圆的面积"""
    return PI * radius ** 2

# 模拟配置
class Config:
    """配置类"""
    DATABASE_URL = "sqlite:///database.db"
    DEBUG_MODE = False
    MAX_RETRIES = 3

# 使用配置
print(f"数据库连接: {Config.DATABASE_URL}")
print(f"调试模式: {Config.DEBUG_MODE}")
```

---

## 2.7 综合示例

### 示例 1：学生信息管理系统（简化版）
```python
"""
学生信息管理 - 变量与数据类型综合示例
"""

# 学生信息
student_name = "李小明"
student_age = 20
student_id = "20230001"
is_enrolled = True
courses = ["Python编程", "数据结构", "算法设计"]
scores = {"Python编程": 95, "数据结构": 88, "算法设计": 92}

# 显示学生信息
print("=" * 40)
print("学生信息管理系统")
print("=" * 40)
print(f"姓名: {student_name}")
print(f"年龄: {student_age}")
print(f"学号: {student_id}")
print(f"在籍状态: {'在读' if is_enrolled else '已毕业'}")

# 显示课程成绩
print("\n课程成绩:")
total_score = 0
for course, score in scores.items():
    print(f"  {course}: {score}分")
    total_score += score

# 计算平均分
average_score = total_score / len(scores)
print(f"\n平均成绩: {average_score:.1f}分")

# 成绩评价
if average_score >= 90:
    evaluation = "优秀"
elif average_score >= 80:
    evaluation = "良好"
elif average_score >= 60:
    evaluation = "及格"
else:
    evaluation = "不及格"

print(f"评价: {evaluation}")
print("=" * 40)
```

### 示例 2：温度转换器
```python
"""
摄氏度与华氏度转换器
"""

# 常量定义
CONVERSION_FACTOR = 1.8
FAHRENHEIT_OFFSET = 32

# 用户输入
celsius_input = input("请输入摄氏温度: ")

# 类型转换和验证
if celsius_input.replace('.', '', 1).isdigit():  # 检查是否是数字
    celsius = float(celsius_input)
    
    # 温度转换公式：F = C × 1.8 + 32
    fahrenheit = celsius * CONVERSION_FACTOR + FAHRENHEIT_OFFSET
    
    # 输出结果
    print("=" * 30)
    print("温度转换结果")
    print("=" * 30)
    print(f"摄氏温度: {celsius:.1f}°C")
    print(f"华氏温度: {fahrenheit:.1f}°F")
    
    # 温度评价
    if celsius < 0:
        feeling = "❄️ 寒冷"
    elif celsius < 15:
        feeling = "🍂 凉爽"
    elif celsius < 25:
        feeling = "🌤️ 舒适"
    elif celsius < 35:
        feeling = "☀️ 温暖"
    else:
        feeling = "🔥 炎热"
    
    print(f"体感: {feeling}")
    print("=" * 30)
else:
    print("错误: 请输入有效的数字！")
```

---

## 2.8 本章练习

### 练习 1：变量操作
创建一个程序，完成以下操作：
1. 创建三个变量分别存储你的姓名、年龄、城市
2. 使用 f-string 格式化输出：`"我叫[姓名]，今年[年龄]岁，来自[城市]。"`
3. 将年龄增加 5 岁，重新输出信息
4. 尝试将年龄转换为字符串，并与"岁"拼接

### 练习 2：数据类型判断
编写程序，要求用户输入任意内容，然后判断并输出：
1. 输入内容的类型
2. 如果是数字，判断是整数还是浮点数
3. 如果是字符串，输出它的长度
4. 如果是空字符串，输出"空输入"

### 练习 3：单位转换器
创建一个小型单位转换器，包括：
1. 千米 ↔ 英里（1 千米 = 0.6214 英里）
2. 千克 ↔ 磅（1 千克 = 2.2046 磅）
3. 摄氏度 ↔ 华氏度

要求用户选择转换类型，输入数值，输出转换结果。

### 练习 4：用户注册验证
模拟用户注册，要求输入：
1. 用户名（必须大于3个字符）
2. 密码（必须至少8位，包含字母和数字）
3. 邮箱（必须包含"@"和"."）

验证输入是否符合要求，给出相应的提示信息。

### 练习 5：购物车计算
创建一个简单的购物车程序：
```python
# 商品信息
item1_name = "Python书籍"
item1_price = 59.99
item1_quantity = 2

item2_name = "编程鼠标"
item2_price = 129.50
item2_quantity = 1

# 计算：
# 1. 每种商品的总价
# 2. 购物车总价
# 3. 如果总价超过200元，打9折
# 4. 显示详细信息（商品名、单价、数量、小计）
# 5. 显示总计和折扣信息
```

### 练习 6：数据类型挑战
下面的代码会输出什么？先思考，再运行验证：
```python
a = 10
b = "20"
c = True

result1 = a + int(b)
result2 = str(a) + b
result3 = a + c
result4 = bool(a) and bool(b)

print(f"result1: {result1}, type: {type(result1)}")
print(f"result2: {result2}, type: {type(result2)}")
print(f"result3: {result3}, type: {type(result3)}")
print(f"result4: {result4}, type: {type(result4)}")
```

---

## 总结

本章中，你学习了：
- ✅ **变量**的概念和命名规则
- ✅ Python 的**基本数据类型**：int, float, str, bool, NoneType
- ✅ 如何**查看和转换**数据类型
- ✅ Python 的**动态类型**特性和**类型注解**
- ✅ **常量**的约定用法

**关键要点**：
1. Python 变量不需要声明类型，可以动态改变
2. 使用有意义的变量名（snake_case 风格）
3. 理解不同数据类型的特性和使用场景
4. 掌握类型转换的方法和注意事项
5. 使用类型注解提高代码可读性

**常见问题提醒**：
- 字符串索引从 0 开始
- 浮点数计算可能有精度问题（如 `0.1 + 0.2 != 0.3`）
- `None` 表示空值，使用 `is` 进行比较
- 布尔值 `True` 和 `False` 首字母大写

---

## 扩展知识
- [Python 数据类型官方文档](https://docs.python.org/zh-cn/3/library/stdtypes.html)
- [PEP 8 编码规范](https://www.python.org/dev/peps/pep-0008/)
- [Python 类型注解指南](https://docs.python.org/zh-cn/3/library/typing.html)

---
**下一章**：[第3章：运算符与表达式](./03-operators-and-expressions.md)

> 如果有任何问题，欢迎在 [Issues](https://github.com/SuperF2023/python-basics-tutorial/issues) 中提问！